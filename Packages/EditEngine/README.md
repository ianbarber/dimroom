# EditEngine

Core Image filter graph and `EditState` Codable structure for non-destructive photo editing. Builds a GPU-backed pipeline that serves both the live preview in the develop view and final export rendering.
