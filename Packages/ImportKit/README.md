# ImportKit

Handles photo import from multiple sources (ImageCaptureCore for tethered cameras, folder-based for film scans). Deduplicates by SHA-256 content hash, extracts EXIF metadata, and stages files for cataloging and upload.
